name: Release

on:
  push:
    tags:
      - "*"

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version: 20

      - name: Check tag matches manifest.json version
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          MANIFEST_VERSION=$(node -p "require('./manifest.json').version")
          if [ "$TAG" != "$MANIFEST_VERSION" ]; then
            echo "Tag ($TAG) does not match manifest.json version ($MANIFEST_VERSION)."
            exit 1
          fi

      # --legacy-peer-deps: vitest's @types/node peerOptional range (^20/^22/>=24) conflicts
      # with this project's own @types/node@^16.11.6 devDependency. The conflict is type-only
      # (peerOptional, not a real runtime dependency), so it's safe to bypass here the same way
      # local installs do.
      - run: npm ci --legacy-peer-deps

      - run: npm run build

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            main.js
            styles.css

      - name: Create release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${GITHUB_REF#refs/tags/}" \
            main.js manifest.json styles.css \
            --title "${GITHUB_REF#refs/tags/}" \
            --generate-notes
